package com.officegain.enigma.base

class ProductReward {

    Long productId
    Long customerGroupId
	Integer points

	static mapping = {
		id column: "product_reward_id"
		version false
	}

    static constraints = {
        points nullable: true
    }
}
